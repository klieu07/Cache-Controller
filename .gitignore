# Verilator build output (compiled binaries, obj_dir contents, logs)
build/

# Waveform dumps -- generated by simulation, not source
*.vcd

# Sweep output -- regenerate with sweep.sh rather than committing stale numbers
results.csvresults.csv
*.vcd
